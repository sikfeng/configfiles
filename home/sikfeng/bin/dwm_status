#!/bin/bash

print_mem(){
  memfree=$(($(grep -m1 'MemAvailable:' /proc/meminfo | awk '{print $2}') / 1024))
  echo -e "$memfree"
}

xsetroot -name "  $(~/bin/temperature) | $(~/bin/wifi) | $(~/bin/volume) | $(~/bin/battery) | $(~/bin/date)"
